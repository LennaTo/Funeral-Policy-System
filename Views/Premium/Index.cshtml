@model IEnumerable<FuneralPolicyApp.Models.Premium>

<p>
    @Html.ActionLink("Pay Premium", "AddPremium")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PremiumAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PolicyNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PaymentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.PremiumAmount)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.PolicyNo)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.PaymentDate)
    </td>
    
</tr>
}

</table>

<p>
    @Html.ActionLink("Pay Premium", "AddPremium")
</p>
